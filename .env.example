# ─── Supabase ─────────────────────────────────────────────
SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOi...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...

# ─── MySQL Hostinger (tokens ML cifrados) ─────────────────
DB_HOST=srv1249.hstgr.io
DB_PORT=3306
DB_NAME=u531713409_kubera_ml
DB_USER=u531713409_brandon2026
DB_PASSWORD=YOUR_DB_PASSWORD
DB_ENCRYPTION_KEY=YOUR_FERNET_KEY

# Ajusta si tu tabla/columnas tienen otros nombres
ML_TOKENS_TABLE=ml_tokens
ML_TOKENS_ID_COL=ml_user_id
ML_TOKENS_NICK_COL=nickname
ML_TOKENS_ACCESS_COL=access_token
ML_TOKENS_REFRESH_COL=refresh_token
ML_TOKENS_EXPIRES_COL=expires_at

# ─── ML OAuth (para refresh automático del token) ─────────
ML_CLIENT_ID=
ML_CLIENT_SECRET=

# ─── Cron ─────────────────────────────────────────────────
# Genera uno: `python -c "import secrets;print(secrets.token_urlsafe(32))"`
CRON_SHARED_SECRET=change-me

# Si quieres correr el cron desde el propio backend (en vez de pg_cron):
ENABLE_LOCAL_SCHEDULER=false
LOCAL_SCHEDULER_HOUR=3
LOCAL_SCHEDULER_MINUTE=0

# ─── CORS / Frontend ─────────────────────────────────────
FRONTEND_ORIGIN=http://localhost:5173

DEFAULT_WEEKLY_GOAL_MXN=1800000
